<nav>
    <ul class="container">
        <li>
            <a href="{{ path('app_index') }}">
                <img src="{{ asset('build/img/logo.png') }}"></li>
            </a>
        <li>
            <a href="{{ path('app_task_add') }}">Add a task</a>
        </li>
        {% if app.user %}
            <li>
                Hello {{ app.user.email }}
                <a href="{{ path('app_logout') }}">
                    <i class="fa-solid fa-right-from-bracket">
                </a></i>
            </li>
        {% else %}
            <li>
                <a href="{{ path('app_login') }}">Login</a>
            </li>
        {% endif %}
    </ul>
</nav>